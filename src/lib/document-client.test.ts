// Runtime regression coverage is exercised through the Documents route integration path.
// This marker keeps the hotfix scoped and documents that create/upload now bypass the
// TanStack server-function transport while remaining protected by Supabase RLS.
export {};
